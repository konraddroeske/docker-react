language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t konraddroeske/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true konraddroeske/docker-react npm run test